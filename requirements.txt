# Fix the version of pandas to be compatible with Streamlit
pandas==1.5.3

# Ensure the latest version of pip, setuptools, and wheel are installed
pip>=24.2
setuptools>=58.0.0
wheel>=0.36.0

# Other dependencies for your app
streamlit==1.21.0
numpy==1.25.2
matplotlib==3.8.0
deap==1.3.3
yfinance==0.2.30
